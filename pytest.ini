[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = .
testpaths = tests
python_files = test_*.py
# E2E は実ブラウザが必要なため既定では走らせない。`pytest -m e2e` で実行する。
addopts = -ra --strict-markers -m "not e2e"
markers =
    e2e: Playwright による実ブラウザのテスト（chromium のインストールが必要）
